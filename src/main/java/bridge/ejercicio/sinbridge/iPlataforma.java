package bridge.ejercicio.sinbridge;

public interface iPlataforma {
    void nombre();
    void reproductor();//Windows unicamente
    //Si queremos que Windows haga un reproductor (no Linux, hay lío)
}
